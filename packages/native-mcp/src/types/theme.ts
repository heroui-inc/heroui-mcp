/**
 * Theme System Types - Simplified for Native
 */

import type {NativeTheme, NativeThemeSystem} from "../lib/native-types";

// Re-export types for service compatibility
export type ThemeSystem = NativeThemeSystem;
export type ThemeDefinition = NativeTheme;
